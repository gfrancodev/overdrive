---
description: Execute an approved Overdrive spec using the best-fit normal or subagent-driven engine
---
Use `execute-plan`. Read the spec as the source of truth, analyze task independence/dependencies/shared files,
recommend the best execution engine, then execute after the user's choice unless auto-run is active.
